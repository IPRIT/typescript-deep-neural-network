/**
 * @param {boolean} crossNeeded
 * @param {number} classesNumber
 * @param {number} rank
 * @param {number} pointsNumber
 * @param {number} minDistanceBetween
 * @returns {Array} Array of classes
 */
export const generate = require('../Lab01/class_generation').generate;